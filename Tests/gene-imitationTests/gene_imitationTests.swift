import XCTest
@testable import gene_imitation

class gene_imitationTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssertEqual(gene_imitation().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
